{
  "name": "emby-theater-enhanced-tooling",
  "version": "0.2.2",
  "private": true,
  "description": "Local build tooling; does not replace the frozen Electron runtime",
  "scripts": {
    "test": "node --test tests/*.test.cjs"
  },
  "devDependencies": {
    "node-unrar-js": "2.0.2"
  },
  "dependencies": {
    "@grpc/grpc-js": "1.14.4",
    "@grpc/proto-loader": "0.8.1"
  }
}
